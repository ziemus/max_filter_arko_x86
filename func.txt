section .text

global filter_x86
global _filter_x86

filter_x86:
_filter_x86:
	push	ebp
	mov		ebp,	esp

	mov		eax,	0
	mov		esi,	[ebp+8]
	mov		edi,	[ebp+12]
init_var:	
	push	0
	cmp		eax,	11
	add		eax,	l
	jl		init_var

;quitting
	mov		eax,	0
quit:	
	pop		ebp
	cmp		eax,	12
	add		eax,	1
	jl		quit

	ret